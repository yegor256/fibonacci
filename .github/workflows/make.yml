name: make
on:
  push:
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
      - run: sudo apt-get update -y
      - run: sudo apt-get install -y clang bc
      - run: make clean
      - run: make summary.csv
