name: make
on:
  push:
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
      - uses: KyleMayes/install-llvm-action@v1
        with:
          version: "13"
      - run: sudo apt-get update -y
      - run: sudo apt-get install -y cppcheck bc sbcl
      - run: make clean
      - run: make env
      - run: make WANTED=1024 summary.csv
      - run: make sa
